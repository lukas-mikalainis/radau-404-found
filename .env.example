# Optional values. Copy to .env; npm start loads it with Node's built-in loader.
PORT=3001
# Defaults are data/ and uploads/ inside the project. For deployments, use
# absolute persistent paths if the release directory changes. Keep real paths private.
# DATA_DIR=
# UPLOAD_DIR=
# Seed six fictional examples ONLY if the database is empty.
SEED_DEMO=0
